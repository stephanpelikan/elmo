import styled from 'styled-components'
import { Badge } from '../components/Badge';

const CardBadge = styled(Badge)`
  position: absolute;
  right: -0.5rem;
  top: -0.5rem;
`;

export { CardBadge };
